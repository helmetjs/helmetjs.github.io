baseURL = 'https://helmetjs.github.io'
locale = 'en-us'
title = 'Helmet'
disableKinds = ['taxonomy', 'term', 'sitemap']

[markup]
  [markup.goldmark.renderer]
    unsafe = true
  [markup.highlight]
    style = 'onedark'
